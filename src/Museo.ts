export default interface Museo {
  location: { lat: number; lon: number };
  name: string;
  topic: string;
  phone: string;
  img: string;
  url: string;
  desc: string;
  id: string;
  address: string;
}
